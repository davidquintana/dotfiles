# Dotfiles Notes

Copy shared configs into `$HOME`:
```
./install.sh
```

Existing files that differ are backed up under `~/.dotfiles-backup/`.

Mac Homebrew Brewfile generated with:
```
brew bundle dump --global --force --describe
```

Homebrew installed 
```
brew list --installed-on-request
```
